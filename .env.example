# For MilesWeb deployment, use 127.0.0.1 instead of 'vivantara.com'
# Example: DATABASE_URL=mysql://USER:PASSWORD@127.0.0.1:3306/DBNAME
DATABASE_URL=mysql://USER:PASSWORD@127.0.0.1:3306/DBNAME
SESSION_SECRET=change-me-to-a-long-random-string

# Optional
PORT=3000
HOST=0.0.0.0
NODE_ENV=production
SEED_ADMIN_PASSWORD=password123

# Email Configuration (SMTP)
# Example for SSL/TLS SMTP (common on cPanel): port 465 + secure=true
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=mail@yourdomain.com
SMTP_PASS=change-me
CONTACT_EMAIL_TO=admin@yourdomain.com
